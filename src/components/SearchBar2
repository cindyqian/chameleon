import React from "react";
import "./SearchBar.css";

const SearchBar = ({ keyword, setKeyword }) => {
  const BarStyling = {
    backgroundColor: "#white",
    width: "100%",
    padding: "20px",
    borderRadius: "30px",
    color: "#6FAEA3",
    opacity: 0.7,
    fontSize: "18px",
    boxShadow: "2px 2px 4px rgba(0, 0, 0, 0.301)",
    border: "none"
  };

  return (
    <div>
      iuhrksjefdehkusjd
      {/* <input /> */}
    </div>
  );
};

export default SearchBar;